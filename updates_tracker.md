# Updates Tracker

Track notable changes, decisions, and progress for the Deadline Extractor project.

## Format

- **Date** — Change description

## Log

- 2026-07-17 — Started frontend dev server (Vite, http://localhost:5173/); created this tracker file.
- 2026-07-18 — Started backend FastAPI server (uvicorn, http://localhost:8000) to fix "Bad Gateway" error on extract (Vite proxy had no backend to forward to).
- 2026-07-18 — Removed top nav bar ("deadline extractor" label and "beta" tag) from App.tsx.
- 2026-07-18 — Added a loading modal with an animated progress bar in UploadPanel.tsx, shown while deadlines are being extracted; added the slide animation keyframes to index.css.
- 2026-07-18 — Added inline rename for extracted deadlines: pen icon next to each deadline's name (DeadlineRow.tsx) opens an editable text field; edits flow up through ResultsPanel to App.tsx state via a new onRename handler.
- 2026-07-18 — Made the rename pen icon always visible (grey), turning black only on direct hover over the icon, instead of appearing on row hover.
- 2026-07-18 — Moved "New input" from a header link into a bottom "Upload another" button next to "Export as .ics calendar file" in ResultsPanel.tsx; header now only shows Copy.
- 2026-07-18 — Filter tabs (ResultsPanel.tsx) now hide when their count is 0 (except "All"), and show counts as "Label (n)" instead of "Label n".
- 2026-07-18 — DeadlineRow.tsx: removed the right-side year, moved year below the date in matching bold/dark style; moved the "Xd overdue"/"in Xd" label under the urgency pill with darker (#777) text for readability.
- 2026-07-18 — Relabeled header "Copy" button to "Copy as text" and added a tooltip clarifying it copies the deadline list as plain text (not the .ics file), since its purpose was ambiguous.
- 2026-07-18 — UploadPanel.tsx: uploaded drop zones now show a clear "filled" state (green border/tint, ✅ icon, filename in green, "Uploaded" label) plus a × button to remove the file, so it's obvious a file was successfully attached.
- 2026-07-18 — Replaced 📄/🖼️/✅ emoji in UploadPanel.tsx with minimal thin-stroke SVG icons (document, image, check) matching the app's monochrome editorial theme; also removed an unused `SANS` const in App.tsx left over from the earlier nav removal (was a tsc error).
- 2026-07-18 — Changed "Upcoming" urgency pill color in DeadlineRow.tsx from grey to pink.
- 2026-07-18 — Sorted deadlines earliest-first in extractor.ts's fetchDeadlines, so the list/filters/copy/.ics export are always chronologically ordered.
- 2026-07-18 — Matched the deadline context text color (DeadlineRow.tsx) to the same grey (#777) as the "in Xd"/"Xd overdue" label, for visual consistency.
- 2026-07-18 — Added "Deadline 6 Dec 2026" stamp decorative graphic to App.tsx's FloatingCards, positioned between the clock and calendar images on the right side.
- 2026-07-18 — Enlarged the stamp graphic (170px → 250px) and raised it to overlap the bottom edge of the clock image.
- 2026-07-18 — Enlarged stamp further (250px → 300px), shifted left, and lowered slightly to reduce overlap with the clock.
- 2026-07-18 — Removed the "Deadline 6 Dec 2026" stamp graphic and its import from App.tsx.
- 2026-07-18 — Replaced the PDF drop-zone icon with a two-tone blue macOS-style folder icon (inline SVG, UploadPanel.tsx); hovering over it animates the front flap dipping/compressing to look like the folder opening slightly.
- 2026-07-18 — Enlarged all floating decorative images (post-its, clock, notepad, calendar) in App.tsx and moved them further toward the center from the page edges.
- 2026-07-18 — Scaled the floating decorative images back down slightly (they were too large after the previous enlargement).
- 2026-07-18 — Replaced the Screenshot drop-zone icon with an iOS-style white document icon (folded corner, grey text lines, inline SVG in UploadPanel.tsx), replacing the old image/mountain icon.
- 2026-07-18 — Swapped the bottom-left decorative notepad image in App.tsx for a new yellow spiral notebook image.
- 2026-07-18 — Enlarged the notebook image (260px → 300px) and moved it further left.
- 2026-07-18 — Darkened the Screenshot drop-zone document icon's outline/lines and added a drop shadow (UploadPanel.tsx) since the white icon was nearly invisible against the cream zone background.
- 2026-07-18 — Added a subtle hover animation (slight lift + tilt) to the Screenshot drop-zone document icon in UploadPanel.tsx, matching the PDF folder's hover treatment.
- 2026-07-18 — Added a "No deadlines found" modal (UploadPanel.tsx) shown when extraction returns zero deadlines, with a "Reupload" button that clears the current file/text so the user can try again.
- 2026-07-18 — Changed "Upcoming" urgency pill color in DeadlineRow.tsx from pink to purple.
- 2026-07-19 — Diagnosed why screenshot uploads found no deadlines: the local Ollama vision model (gemma4:e2b/latest) hallucinates unrelated content instead of reading the image — confirmed via direct testing with a synthetic test screenshot (image content ignored regardless of what's in it).
- 2026-07-19 — Added Tesseract OCR (installed via winget, UB-Mannheim build) + pytesseract wrapper (new image_processor.py) so screenshot uploads are OCR'd to text and merged into the existing text pipeline (main.py), instead of being sent to the broken vision model. Removed the now-dead image/vision code path from deadline_extractor.py. Model itself (gemma4:e2b) unchanged. Verified end-to-end with a synthetic test screenshot — OCR + extraction correctly found all 3 deadlines.
- 2026-07-19 — Added a helper note under the upload drop zones: "Works best with typed text — handwriting isn't supported yet." (UploadPanel.tsx).
- 2026-07-19 — Added an OCR-quality gate: image_processor.py's new `is_text_usable()` checks OCR output is ≥20 chars and ≥40% alphanumeric before it's sent to the LLM. If a screenshot's OCR text fails this check and there's no other content (PDF/text) to fall back on, main.py returns `unreadable_image: true` and skips the LLM call entirely. Added a distinct `UnreadableImageModal` in UploadPanel.tsx ("We couldn't read this image — it may be handwritten or low resolution. Add the deadline manually?") with a manual title+date entry form and a reupload option, kept separate from the existing "No deadlines found" modal (which still triggers when OCR text is usable but no deadlines are found). Verified both paths against a live backend with synthetic blank vs. readable test images.
- 2026-07-19 — Replaced the "load example" button in UploadPanel.tsx with a "+ add deadline manually" option that expands an inline title+date form, letting users skip file upload entirely and add a deadline directly.
- 2026-07-19 — Darkened and enlarged the light-grey helper texts in UploadPanel.tsx ("Click or drag & drop", the handwriting note, "or paste text directly", "+ add deadline manually") for readability; the paste and manual-entry toggles now match at text-sm.
- 2026-07-19 — Moved the handwriting note from a standalone line below both drop zones into the Screenshot zone specifically, wrapped in parentheses: "(works best with typed text — handwriting isn't supported yet)"; it hides once a screenshot is uploaded.
- 2026-07-19 — Cleaned up the handwriting note's styling (italic, muted single color, tighter line-height, narrower max-width) since the wrapped two-line text looked messy against the box.
- 2026-07-19 — "Add deadline manually" in UploadPanel.tsx now supports multiple entries: each row has its own title/date with a remove button, a "+ add another" link appends new rows, and submitting batches all valid rows into one onExtract call ("Add N deadlines"). Also fixed a potential ID collision in extractor.ts's makeManualDeadline (was `manual-${Date.now()}` alone, which could collide when multiple entries are created in the same tick — added a random suffix).
- 2026-07-19 — Added a thin dashed divider between manual deadline entry groups in UploadPanel.tsx so consecutive title/date pairs are visually distinguishable.
- 2026-07-19 — Made raw model output logging permanent (per user request, for their Model Limits Log documentation): deadline_extractor.py now prints every raw model response and appends it to logs/model_outputs.txt with an ISO timestamp and source filename (passed through from main.py as pdf/screenshot filename, or "pasted text").
- 2026-07-19 — Added today's date to the system prompt in deadline_extractor.py (injected at call time via datetime.now(), not hardcoded) so the model can resolve relative/partial dates ("next Friday", dates without a year) correctly. Verified: "next Friday" from a test run correctly resolved to a real upcoming Friday in 2026.
- 2026-07-19 — Added a `year_inferred` field to the model's output schema (true when the source text didn't state a year explicitly) and a post-processing step (`_resolve_deadline_year`) that rolls the year forward until the date is >= today, but only when year_inferred is true — dates with an explicit year (even if genuinely in the past/overdue) are left untouched. The `year_inferred` field is stripped before the deadline reaches the API response. Verified all three cases: inferred+past rolls forward, explicit+past stays, inferred+future stays.
- 2026-07-19 — Fixed "+ add deadline manually" in UploadPanel.tsx disappearing once text was typed/pasted or a file uploaded — it was wrongly gated behind `!canExtract`; now always visible.
- 2026-07-19 — Added code-driven (not prompt-driven) day-only date resolution in deadline_extractor.py: `_has_explicit_month_for_day()` checks the source text for a month name or numeric slash/dash date near the day-of-month; if the source only gave a bare day number ("the 9th"), the model's guessed month is discarded and `_next_occurrence_of_day()` deterministically computes the next date on/after today with that day-of-month. Sets a new `date_estimated` field (independent of the model's unreliable self-reported `year_inferred`, which is no longer used for any UI decision). Wired `dateEstimated` through extractor.ts and added an amber "date estimated" badge in DeadlineRow.tsx.
- 2026-07-19 — Added `_clean_inference_notes()` in deadline_extractor.py to strip the model's leaked reasoning asides from title/description, e.g. "(Month inferred as July)" — regex-matches parentheticals containing "inferred"/"assumed"/"guessed" while leaving unrelated parenthetical text untouched.
- 2026-07-19 — "No deadlines found" modal now offers both "Add manually" and "Reupload instead" (previously only Reupload), matching the UnreadableImageModal pattern. Extracted the shared title+date form into a new `ManualAddForm` component in UploadPanel.tsx to avoid duplicating it between the two modals.
- 2026-07-19 — Fixed a date-hallucination bug: an entry from "by the 9th" was previously trusted at face value even when the model output a completely different day (e.g. 28th) not present anywhere in the source. deadline_extractor.py now verifies the model's day-of-month is actually verbatim in the source text before trusting it; if not, it searches for the real day near deadline keywords ("due"/"by"/"submit"/etc.) via `_extract_day_candidates()` instead; if no day can be found in the source at all, the entry is routed to a new `unresolved` list (title/description only, no fabricated date) rather than ever reaching the UI with a made-up date. Wired `unresolved` through main.py and extractor.ts, and added a new `UnresolvedItemsModal` in UploadPanel.tsx letting the user fill in a date per unresolved item or skip them. Verified against the exact reported case: source "by the 9th" with model hallucinating Jul 28 → correctly resolved to Aug 9 (next occurrence of the real day 9).
- 2026-07-19 — Added a compact Examples section to SYSTEM_PROMPT_TEMPLATE in deadline_extractor.py covering previously-failing input forms (bare day like "PDS due 9", "report due on 4", explicit day+month "essay due 12 Sep", and a non-deadline sentence returning []). The Python-side day-verification/estimation logic (added earlier the same day) remains the actual safety net regardless of prompt quality — verified the model now closely follows the example pattern, and confirmed post-processing still correctly flags `date_estimated` independent of what the model claims.
- 2026-07-19 — Added a deterministic regex fallback (`_find_fallback_deadlines`) since the model unreliably extracts day-only dates (same input passes/fails across runs). Fires only when the model returns `[]`: matches `(title) (due|submit|deadline) (on|by)? (the)? day(st/nd/rd/th)?` in the source text, resolves the day via the same `_next_occurrence_of_day()` used by the model-driven path (guaranteeing identical dates for identical inputs regardless of which path produced them), uses the text before the keyword as a provisional title, and flags `date_estimated: True`. Verified: fallback and model-driven paths agree on 2026-08-09 for "PDS due 9"; fallback stays silent on genuine non-deadline text; full pipeline test with a mocked empty model response confirms end-to-end engagement.
- 2026-07-19 — Made the regex fallback month-aware: matches month-name+day, day+month-name, numeric slash/dash dates, and optional years, using them verbatim (`date_estimated: false`) instead of blindly running next-occurrence resolution — only a truly bare day number (no month anywhere in the match) triggers that. Also added chunking for long documents: `_chunk_text()` splits into ~2000-char chunks with ~200-char overlap so each model call stays short (JSON compliance held up much better in testing); `_extract_from_chunk()` runs the full per-item pipeline (model call, fallback, date resolution) per chunk, each logged separately as `[chunk N/M]`; results merged and deduped by (title, date). Short inputs are unaffected — single chunk, no log-label change. Verified: all month-aware forms (name+day, day+name, "9/8", "09-08-2026", explicit past year) resolve correctly with date_estimated=false; a mocked long-document test with a deadline sitting at a chunk boundary was correctly caught by the overlap and deduped to one entry; a real course-outline table with 5 explicit-date deliverables extracted all 5 correctly.
- 2026-07-19 — Fixed the model flip-flopping on ambiguous slash dates like "07/08/2026" (confirmed in logs: same repeated input produced both 2026-07-08 and 2026-08-07 across runs). Added `_resolve_ambiguous_day_month()` (shared by both the model-driven and fallback paths): whichever of the two numbers is >12 must be the day (unambiguous, no badge); if both ≤12 it defaults to DD/MM (our users' locale) and is flagged ambiguous. `_resolve_slash_date()` runs as the first check in `_resolve_deadline_date`, overriding the model's date entirely when a slash date is present — regardless of what the model guessed. Added `date_format_ambiguous` field end-to-end (deadline_extractor.py → main.py → extractor.ts → DeadlineRow.tsx) with a distinct amber "date format assumed DD/MM — confirm?" badge, separate from the existing "date estimated" one. Added inline date editing (click either badge to open a date picker in the date column, Enter/blur to save, Escape to cancel) via new `onDateChange` prop threaded through App.tsx → ResultsPanel.tsx → DeadlineRow.tsx and a new `withUpdatedDate()` helper in extractor.ts. Verified: repeated calls with varying model output for the same "07/08/2026" input all deterministically resolve to 2026-08-07; unambiguous cases (25/08, 08/25) correctly show no badge; explicit-month/day-only/explicit-year paths remain unaffected.
- 2026-07-19 — Shortened the ambiguous-date badge to "date format assumed DD/MM" (removed "— confirm?") and fixed its alignment in DeadlineRow.tsx: forced it onto its own full-width line (`basis-full`, `width: fit-content`, `margin: 0`) so it sits flush-left with the description text below instead of wrapping with stray button spacing. Applied the same fix to the "date estimated" badge for consistency.
- 2026-07-19 — Fixed remaining horizontal misalignment: the badge's own `px-2` padding was shifting its text ~8px right of "Assignment due" below it. Offset with a -8px left margin on both badges so their text now starts at the same x-position as the description line.
- 2026-07-19 — Upgraded slash-date resolution to document-level format inference (two-pass), replacing the previous always-DD/MM default. Pass 1 (`_infer_document_date_convention`) scans the whole document once for unambiguous slash dates (one number >12, e.g. "7/22" → MM/DD, "25/08" → DD/MM) to establish the document's convention, returning "mm_dd", "dd_mm", "conflicting" (contradictory evidence found), or "unknown" (no evidence). Pass 2 applies it to every slash date in the document, including ambiguous ones. `_resolve_ambiguous_day_month()` now returns a reason string (forced/inferred/assumed/conflicting) instead of a plain bool, shared by both the model-driven (`_resolve_slash_date`) and fallback (`_resolve_fallback_match`) paths. Convention is computed once from the full document before chunking and threaded through every chunk for cross-chunk consistency. `_SLASH_DATE_RE` now also matches dates without a year (e.g. "7/22"), resolving the year via next-occurrence when absent. New `date_format_reason` field flows end-to-end to DeadlineRow.tsx's `getDateBadge()`, which shows one of three distinct badges: "date format assumed DD/MM" (no document evidence), "format inferred from document" (resolved via convention), or "date format uncertain" (conflicting evidence — every slash date badged, even ones that are individually unambiguous). Verified live via the API: a document with "7/22" (MM/DD evidence) and ambiguous "8/9" correctly inferred MM/DD and resolved "8/9" to 2026-08-09 with reason "inferred".
- 2026-07-19 — Fixed the badge stretching to fill nearly the entire row width: `basis-full` (flex-basis: 100%) on the button itself was overriding its `width: fit-content` inline style, since flex-basis wins over width for a flex item's main-axis size. Moved `basis-full` onto a wrapping div (forcing the line break) and let the button shrink-wrap to its text inside that div.
- 2026-07-19 — Fixed the urgency pill ("Today"/"This week"/"Upcoming") being misaligned with the days-countdown text below it (same px-2 padding offset issue as the date-format badge) — added `-ml-2` to the pill so its text lines up flush-left with "today"/"in Xd" below.
- 2026-07-19 — Changed the "inferred" date-format badge label in DeadlineRow.tsx from "format inferred from document" to "date inferred, click date to change" for clearer call-to-action wording.
- 2026-07-19 — Made the date itself ("Aug 12 / 2026") clickable to open the inline date editor for every deadline, not just ones with a badge — previously only the badge (dateEstimated/dateFormatAmbiguous) triggered editing, so dates without any flag couldn't be corrected inline. Verified against the exact "the 9th"/"the 25th" cases from the model output log: 9th correctly rolled to next August 9, 25th correctly stayed as this coming July 25.
